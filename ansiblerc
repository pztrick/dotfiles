# do we have ansible_hosts defined on this machine?
if [ -f ~/ansible_hosts ];
then
	source ~/.files/ansible/hacking/env-setup
	export ANSIBLE_HOSTS=~/ansible_hosts
	echo -e 'hello \e[1m world \e[0m'
	echo -e '\e[93m\e[1m * * * \e[0m Ansible loaded.'
else
	echo -e "\e[31m\e[1m * * * \e[0mAnsible not detected. (~/ansible_hosts)\e[0m"
fi

echo ""
